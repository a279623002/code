#### 边端智能助手
**功能**：
- 多知识库管理，FAISS嵌入数据库
- 智能切片分块
- 本地Embedding模型
- 多模型管理，OpenAI兼容接口
- rag问答

##### **QA**：

###### 1.怎么管理多知识库？
**答**： 我们使用的是faiss嵌入数据库，mysql进行知识库管理，用知识库id作为faiss向量数据库的分区，切换的时候只有一个知识库处于启用状态，就按id加载本地文件初始化知识库使用

###### 2. 切片策略是什么？
**答**： 基于MarkItDown转换后的标准Markdown格式实现的智能分块策略。(hello-agents chapter8)
    Markdown结构感知的分块流程：
    ```
    标准Markdown文本 → 标题层次解析 → 段落语义分割 → Token计算分块 → 重叠策略优化 → 向量化准备
        ↓                ↓              ↓            ↓           ↓            ↓
    统一格式          #/##/###        语义边界      大小控制     信息连续性    嵌入向量
    结构清晰          层次识别        完整性保证    检索优化     上下文保持    相似度匹配
    ```

###### 3. Embedding模型是什么，为什么选择bge-large-zh-v1.5模型？
**答**： 我们选择bge-large-zh-v1.5模型是因为它在中文文本嵌入任务中表现出色，能够提供高维向量表示，同时保持了模型的轻量级和可部署性。
---

###### 4. 模型管理具体有哪些模型，有什么不同怎么做适配兼容OpenAI？
**答**： 见model-management.md

###### 5. rag召回策略是怎么样的？
**答**： 见rag.md

---

#### 调度系统
**项目说明**： 基于gin框架开发的算力调度系统，用于管理算力资源，包括算力的申请、释放、监控等，通过k8s调度资源算力训练任务以及推理切流。

**功能**：
1. 资源管理：
- 资源配置：配置算力中心、算力集群、算力节点等
- 资源监控：从Prometheus同步算力资源，获取机器ip、卡类型、功耗等指标信息
- 监控面板：配置grafana面板，将prometheus指标数据可视化

2. 任务管理：
- 策略编排：根据任务类型、资源，通过算法编排策略，实现任务的动态调度
- 任务调度：使用调度器，基于k8s调度任务的容器，完成任务调度
- 任务监控：监控任务执行状态，包括任务完成、失败、超时等
- 任务日志：记录任务执行日志，包括任务开始时间、结束时间、执行时间、错误信息等
- 任务工况：记录任务执行过程中，包括算力资源指标如卡类型、功耗、占用内存等，生成任务工况报告上传到存储系统

##### **QA**：
###### 1. 任务编排策略是怎么样的？
**答**： 根据算力资源占用情况，结合电价，通过求解器异步编排任务，当任务失败时，会重新编排任务，确保任务的及时执行，实现资源的动态调度。

###### 2. 任务调度是如何实现的？
**答**： 我们使用的是k8s调度器，根据任务的资源需求，自动选择合适的算力节点，当需要调度到其他节点时，先发起容器中断的请求，重新编排任务，从保存点开始进行训练，完成任务的调度。

###### 3. 任务监控是如何实现的？
**答**： 任务训练时容器上报状态，使用prometheus监控算力资源，获取工况数据，通过配置grafana面板，将prometheus指标数据可视化，实现任务的监控和报警。

---

#### 管控平台
**项目说明**： 基于goframe框架开发的机器人管控平台，用于操作、查看机器人相关信息。

##### 核心功能

1. 机器人管理
- mqtt订阅话题：支持获取机器人运行状态、电量、温度等信息
- mqtt控制话题：支持发送指令到机器人，如动作、表情、行动等

2. 模型管理
- 模型信息录入：针对不同模型配置模型基础信息、不同的核心参数

3. 地图管理
- 地图信息录入：支持上传地图信息，包括地图名称、地图大小、地图坐标等

4. 任务管理
- 任务编排：关联机器人、模型、地图，编排任务执行顺序和条件

